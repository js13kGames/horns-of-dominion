{
  "name": "unicorn-overlord",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.mjs --dev",
    "build": "node build.mjs",
    "size": "node build.mjs --quiet"
  },
  "devDependencies": {
    "@gfx/zopfli": "^1.0.15",
    "esbuild": "^0.25.0",
    "roadroller": "^2.1.0"
  }
}
